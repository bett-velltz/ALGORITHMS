    ios_base::sync_with_stdio(false);
    cin.tie(0)
    coat.tie(0)

    #ifdef _DEBUG
    #include <fstream>
    ifstream cin("input.txt");
    ofstream cout("output.txt";
    #else
    #include <iostream>
    #endif

обязательно использовать везде

итерация, варианты:

    for (auto cur; container) {}
    for (int i = 0; ich; itt) {}
    for (auto cur = con.begin(); cur != con.end(); ++cur) {}