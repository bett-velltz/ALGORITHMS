#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, m;
    long long l;
    cin >> n >> m >> l;
    vector<long long> a(n, 0);
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }
    vector<long long> res;
    for (int i = 0; i < m; ++i) {
       int t;
       cin >> t;
       if (t == 0) {

       } else {
           long long p, d;
           cin >> p >> d;
       }
    }
    return 0;
}