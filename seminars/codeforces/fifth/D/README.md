План:
1. DSU: строим компоненты по существующим рёбрам (ранг счиатть не надо. Ранг нужен только для оптимизации скорости, а здесь n ≤ 1000, поэтому достаточно простого DSU только с расширением пути. Ранг имеет смысл, когда n огромное количество (например, 10⁶) и важна каждая операция. При n ≤ 1000 разница незаметна.)
2. Находим компоненты со столицей и компоненты без столицы (свободные города)
3. Находим максимальную по количеству городов компоненту со столицей. 
4. Подключаем к ней абсолютно все свободные города и компоненты связности, каждый из городов подключая к городу этой компоненты.


читаем n, m, k

инициализируем DSU:
для i от 1 до n: p[i] = i

читаем k столиц в массив capitals[]

читаем m рёбер и объединяем:
для каждого ребра (u, v):
dsu_unite(u, v)

// определяем для каждой компоненты — есть ли в ней столица
массив has_capital[1..n] = false

для каждой столицы c в capitals[]:
root = dsu_get(c)
has_capital[root] = true

// считаем размер каждой компоненты
массив size[1..n] = 0
для i от 1 до n:
root = dsu_get(i)
size[root]++

// все свободные города (компоненты без столицы)
// добавляем к наибольшей компоненте со столицей
найти корень max_root — компонента со столицей с максимальным size

для каждого корня r где has_capital[r] == false и size[r] > 0:
size[max_root] += size[r]
size[r] = 0

// считаем ответ
ans = 0
для i от 1 до n:
если size[i] > 0:
ans += size[i] * (size[i] - 1) / 2

ans -= m   // вычитаем уже существующие рёбра

вывести ans
