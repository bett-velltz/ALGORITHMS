#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>
using namespace std;
using ll = long long;

int main() {
    int n;
    cin >> n;
    vector<ll> a(n);
    vector<ll> b(n);
    vector<ll> t(n);
    for (int i = 0; i < n; ++i) {
        cin >> a[i] >> b[i] >> t[i];
    }


    return 0;
}